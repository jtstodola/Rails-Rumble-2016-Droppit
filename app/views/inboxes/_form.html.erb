<%= form_for(@inbox) do |f| %>
  <% if @inbox.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inbox.errors.count, "error") %> prohibited this inbox from being saved:</h2>

      <ul>
      <% @inbox.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :remote_folder %><br>
    <%= f.text_field :remote_folder %>
  </div>
  <div class="field">
    <%= f.label :service %><br>
    <%= f.select :service, [["Dropbox", "dropbox" ]] %>
  </div>
  <div class="field">
    <%= f.label :expire %><br>
    <%= f.datetime_select :expire %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
